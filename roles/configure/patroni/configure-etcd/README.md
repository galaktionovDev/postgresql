# Configure etcd

## Description
A role to install and configure etcd.

## Input variables

This role uses the following variables, that intended to be overriden:
- `etcd_home_directory` - the home directory of the `etcd` system user. The default is `/var/lib/etcd`